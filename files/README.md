# web/files/

Drop downloadable files here, then add an entry to `../data/downloads.json`
pointing at `files/<name>`. See `../README.md` for the entry format.

This folder is empty on purpose — the REX installers live in `dist-installer/`
at the repo root and are linked from there.
